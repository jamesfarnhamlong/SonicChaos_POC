v09 focuses on contact/state consistency: the spring already at (3200,832) now uses its full block contact instead of a centre-only test, and uses the ROM +6 speed; spin uses the same ramp handling as the normal player. Both-release braking and roll friction now follow velocity sign. The tile dispatch for the twisting strip is extracted verbatim to movement09_rom.json but is NOT active yet: its handler depends on the original player position integration, so using it as a path without that conversion would misrepresent the ROM. The first ramp and strip still need play testing and further engine work. No GameMaker runtime is available here.
