v10 uses the ROM Act 1 object records: object type $26 is placed at (688,864), (3568,768), (1296,608), and (1912,864), with parameter zero using Y=-7.375 and other parameters using Y=-5 according to bank-30 handler. Its art is a palette-adjusted stand-in for the gold original. The ROM terrain spring uses Y=-7.5; Chaos upright spring flight uses the 0x20 fixed-point acceleration (0.125 pixels per update). One-way collision flag $41 now resolves an above-to-below crossing before the lower solid tile can trap Sonic, with a matching End Step fallback. The twisting strip remains a separate future task. Not run inside GameMaker.
