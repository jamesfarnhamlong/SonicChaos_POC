// The verified placement anchor is (3960,558). At this THZ1 placement the
// decoded ground surface is y=580, so presentation settles 22 pixels lower
// without changing the canonical room record or the verified animation.
draw_sprite(sprite_index, floor(image_index), x, y + 22);
